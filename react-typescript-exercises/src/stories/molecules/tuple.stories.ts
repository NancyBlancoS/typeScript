import type { Meta, StoryObj } from '@storybook/react';

import { Tuple } from '../../quiz-1'

export default {
    title: 'Tuple',
    component: Tuple,
} as Meta <typeof Tuple>;

export const Primary: StoryObj<typeof Tuple> ={
}